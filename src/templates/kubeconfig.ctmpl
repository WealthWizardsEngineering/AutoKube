{{- $password := env "KUBERNETES_PASSWORD" -}}
{{- $username := env "KUBERNETES_USERNAME" -}}
apiVersion: v1
kind: Config
users:
- name: {{ $username }}
  user:
    password: {{ $password }}
    username: {{ $username }}